# Backend environment (Node / Express)
#
#   server/.env.development   — local `pnpm dev`
#   server/.env.production    — production Node (optional locally; Render uses UI)
#   server/.env               — optional fallback (non-mode-specific)
#
# Templates: `.env.development.example`, `.env.production.example`
