# Frontend environment (Vite)
#
#   client/.env.development   — `pnpm dev`
#   client/.env.production    — `vite build`
#
# Templates: `.env.development.example`, `.env.production.example`
# Configure Vite with envDir: client/ (see vite.config.ts).
