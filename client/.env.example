# Copy to `client/.env` (gitignored). Used for both `pnpm dev` and `vite build`.
# Netlify / CI can set the same keys in the dashboard instead of a file.

# Public URLs (safe to expose in the client bundle)
VITE_PUBLIC_SITE_URL=https://twquantus.netlify.app
# VITE_PUBLIC_API_URL=https://t-w-quantus.onrender.com

# VITE_PUBLIC_BUILDER_KEY=
